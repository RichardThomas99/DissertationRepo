import React from 'react';
import'../App.css';

/**
 * A ReactJS class which supplies the text for the over time portion of the software.
 * @example
 * return (
 *     <div>
 *      <p>Each listing is scored out of 100 so the better the listing the closer to 100 it is.</p>
 *     </div>
 * )
 */
function OverTimeText()
{

  return(
    <div>
      <h4>This Over-Time section presents analysis and provides insight into the long-term trends of the data stored in the database..</h4>
    </div>
  );
}

export default OverTimeText;
